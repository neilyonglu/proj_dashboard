# Secrets / environment
.env

# Claude Code project instructions (local only)
CLAUDE.md

# Runtime data (database + backups + logs + uploaded avatars)
instance/
data/
static/avatars/*
!static/avatars/.gitkeep

# Python
__pycache__/
*.pyc

# CodeGraph local index (not for version control)
.codegraph/