# Secrets / environment
.env

# Claude Code project instructions (local only)
CLAUDE.md

# Build output
*.exe

# Runtime data (database + auto backups + uploaded avatars are local)
instance/

# Python
__pycache__/
*.pyc

# PyInstaller build artifacts
build/
build_tmp/
dist/
*.spec

# CodeGraph local index (not for version control)
.codegraph/
