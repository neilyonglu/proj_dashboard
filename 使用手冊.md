# 專案派工進度系統 — 使用手冊

> 版本：v1.1.1
> 適用對象：日常操作人員（新增工時、管理專案、查看進度與統計）

---

## 目錄

1. [啟動與開啟系統](#1-啟動與開啟系統)
2. [首頁導覽](#2-首頁導覽)
3. [工作內容填表（新增工時）](#3-工作內容填表新增工時)
4. [員工表（個人工時）](#4-員工表個人工時)
5. [專案進度表（表格／時間軸）](#5-專案進度表表格時間軸)
6. [加班統計](#6-加班統計)
7. [資料庫管理（需密碼）](#7-資料庫管理需密碼)
8. [管理參與人員（含辭職）](#8-管理參與人員含辭職)
9. [管理業務代表／專案種類](#9-管理業務代表專案種類)
10. [資料備份與還原](#10-資料備份與還原)
11. [操作日誌](#11-操作日誌)
12. [常見問題](#12-常見問題)

---

## 1. 啟動與開啟系統

系統在電腦上啟動後，請用瀏覽器開啟：

```
http://localhost:5001
```

> 給技術人員：本專案在 conda 環境 `proj_dash` 執行，啟動指令
> `conda run -n proj_dash python app.py`。若 5001 埠被舊程式佔用，需先關閉舊程式。

畫面右上角會顯示目前的**版本號**（例如 `v1.0.1`）。

---

## 2. 首頁導覽

首頁上方有三個即時統計：

| 卡片 | 意義 |
|---|---|
| 進行中專案 | 狀態為「進行中」的專案數量 |
| 本月投入工時 | 本月所有工時紀錄的「工作天數」加總 |
| 登錄員工數 | 系統內的員工總數 |

下方有五個功能入口：**工作內容填表、員工表、專案進度表、加班統計、資料庫管理**。

> **注意**：未登入管理者時，「資料庫管理」卡片會顯示鎖頭圖示，點擊後導向登入頁。登入成功後回到首頁才會出現完整入口。

---

## 3. 工作內容填表（新增工時）

首頁 →「**工作內容填表**」。

1. **參與人員**：點欄位後可**直接打字篩選**名單，或從清單點選。
   > 已辭職的員工仍可選取（方便補登舊資料），會標示「已辭職 日期」。
2. **專案列表**：同樣可**打字篩選**或點選現有專案。
3. **工作日期**：選擇日期。
4. **工作天數**：支援 0.5 為單位（例如 1、0.5）。
5. **時數明細（選填）**：可填日班／加班／夜班時數，與工作天數並存。
   > 「加班統計」頁面就是統計這裡填的日班／加班／夜班時數。
6. **工作內容**：必填，詳述當天工作。
7. **備註**：選填。
8. 按 **送出**。

> 注意：人員與專案**必須從清單中選到有效項目**才能送出（打字但沒選到會被清空，避免錯字資料）。

---

## 4. 員工表（個人工時）

首頁 →「**員工表**」。

- 上方選擇員工，即可看到該員工的所有工時紀錄、總工作天數、參與專案數。
- 從這裡也可快速跳到「新增工時」並自動帶入該員工。

### 分組顯示

工作紀錄表右上方有切換按鈕：**全部 ｜ 月 ｜ 週 ｜ 日**。

| 模式 | 說明 |
|---|---|
| 全部 | 全部攤開，不分組 |
| 月 | 依年月分組（預設） |
| 週 | 依 ISO 週次分組 |
| 日 | 依單日分組 |

- 每個分組標頭顯示**筆數、工作天數合計、日/加/夜時數合計**。
- 點標頭可**展開／收合**該組；右側「收合全部／展開全部」可一鍵操作。
- 選擇的模式會記憶在瀏覽器，下次開啟相同。

### 刪除權限

- **一般使用者**：只能查看與編輯工時紀錄，**不會看到刪除按鈕**。
- **管理者**（已輸入資料庫密碼）：才會顯示刪除按鈕，且後端同步驗證，直接呼叫 API 也會被擋。

---

## 5. 專案進度表（表格／時間軸）

首頁 →「**專案進度表**」。頁面有兩個分頁：

### 專案表格
- 以表格列出所有專案：名稱、期間、狀態、業務代表、設備、人員名單、內容、備註。
- 表頭的「業務代表／狀態／人員名單」可下拉**篩選**。
- 上方可用**關鍵字搜尋**與**月份區間**篩選。
- 「**新增自訂欄位**」可自行增加欄位（僅存在本機瀏覽器）。
- 滑到某列右側可點鉛筆**編輯專案**。

### 時間軸（甘特圖）
- 以橫條呈現每個專案的期間，顏色代表狀態。
- 藍色小條是各筆工時，滑鼠移上去會顯示**人員、日期、時數、內容**。
- 可**縮放**（月／季／年）、回到今天、顯示全部。

### 辭職員工標記
- 人員名單中若有**已辭職**的員工，會以**紅底**顯示「名字 · 離職 日期」。
- 時間軸的工時條上，辭職員工名字後會有「⚠」，滑鼠移上去顯示辭職日期。

> 新增專案：本頁右上「**新增專案**」按鈕。

---

## 6. 加班統計

首頁 →「**加班統計**」。

1. 上方選擇**統計區間**：
   - **依日／依月／依年**：選好後可用左右箭頭切換上一／下一個區間。
   - **自訂區間**：選此項會出現「起始日 ~ 結束日」兩個日期欄，選好按「查詢」即可（頭尾日期都會算進去，例如 2026/02/01 ~ 2026/03/01）。
2. 上方四張卡顯示該區間的**日班、加班、夜班總時數**與**有時數筆數**。
3. 下方兩張表：
   - **依人員統計**：每位員工的日班、加班、夜班時數與筆數。
   - **依專案統計**：每個專案的日班、加班、夜班時數與筆數。
   - 皆依加班時數由高到低排序。

> 沒有數字時：代表該區間的工時紀錄都沒有填「時數明細」。請在新增/編輯工時的「時數明細」中填入日班／加班／夜班時數。

---

## 7. 資料庫管理（需密碼）

首頁 →「**資料庫管理**」，需輸入後台密碼。

> 密碼由系統管理者於 `.env` 的 `DB_ADMIN_PASSWORD` 設定（目前為 `5206`）。

進入後有五個分頁：**專案、工作紀錄、業務代表、參與人員、專案種類**。

### 專案分頁
- **搜尋**：依專案名稱。
- **狀態過濾**：下拉選「進行中／等待中／暫緩中／已結案」只看該狀態（可與搜尋同時使用）。
- 每列右側可**編輯**或**刪除**專案。
  > ⚠ 刪除專案會**連同其所有工時紀錄一起刪除**，且無法復原（會先跳出確認）。

### 工作紀錄分頁
- 可搜尋、編輯、刪除單筆工時。

### 匯入／匯出（CSV）
- 各分頁右上有「**匯出**」（下載 CSV）與「**匯入**」（上傳 CSV）。
- 匯入時可選「**略過重複**」或「**覆蓋更新**」。
- CSV 支援 UTF-8 / BIG5（相容 Excel）。

---

## 8. 管理參與人員（含辭職）

路徑：資料庫管理 → **參與人員**分頁 → 「**管理參與人員**」按鈕
（或新增工時表單裡「參與人員」右上的「管理名單」連結）。

### 新增人員
- 填「系統代號（必填）」「顯示名稱（選填）」，可上傳大頭貼，按「新增人員」。

### 編輯人員 / 設定辭職
1. 在清單找到該員工，點右側**鉛筆（編輯）**圖示展開表單。
2. 表單中有 **「辭職日期（在職請留空）」** 欄位，選擇日期。
3. 按 **儲存變更**。
4. 之後該員工會顯示紅色「**已辭職 日期**」標籤，並在專案進度表中標記。

> **恢復在職**：回到編輯，把辭職日期**清空**再儲存即可。

> 改「系統代號」會自動同步更新該員工所有的工時紀錄。

### 移除人員
- 點垃圾桶圖示。移除不會刪除其工時紀錄，但未來無法再被選取。
  > 建議：若員工只是離職，請用「辭職日期」而非移除，這樣歷史紀錄仍清楚標示。

---

## 9. 管理業務代表／專案種類

路徑：資料庫管理 →「業務代表」或「專案種類」分頁 → 對應的管理按鈕。

- 可新增、編輯、刪除。
- 編輯名稱時，會**自動同步更新**所有使用到該名稱的專案。

---

## 10. 資料備份與還原

- **自動備份**：每次系統啟動會自動備份資料庫，只保留最近 10 份。
- **立即備份**：資料庫管理頁右上「立即備份」。
- **下載備份**：右上「下載備份」會下載目前資料庫檔（`.db`）。
- **還原備份**：右上「還原備份」上傳 `.db` 檔；還原前會先自動備份目前資料再覆蓋。

---

## 11. 操作日誌

系統會自動記錄所有**寫入操作**至 `instance/logs/` 目錄，檔名格式為 `activity_YYYYMM_NNN.log`。

**記錄範圍：**
- 管理者登入成功 / 失敗（含 IP）
- 新增、編輯、刪除：工作紀錄、專案、人員、業務代表、專案種類
- CSV 匯入（各類型）
- 手動備份、還原資料庫
- 未授權的刪除嘗試

**日誌格式範例：**
```
[2026-06-27 14:30:00] 192.168.1.5 | 管理者登入成功
[2026-06-27 14:32:11] 192.168.1.5 | 新增工作紀錄 | personnel=Jasper, project=台電南港都更案, date=2026-06-24
[2026-06-27 14:35:00] 192.168.1.8 | 刪除工作紀錄（未授權） | task_id=5
```

- 每個月份自動分檔，每檔上限 **2000 筆**，超過自動開新序號（`_001`, `_002`, ...）。
- 可用記事本或任何文字編輯器（UTF-8）直接開啟查閱。
- 若需要 Task ID 與工作內容的對照，可在 `instance/logs/task_id_reference.md` 查詢（手動產生的快照）。

---

## 12. 常見問題

**Q：新增工時時，人員/專案打了字卻送不出去？**
A：必須從下拉清單中**選到**有效項目（打字只是篩選）。專案無法用打字新建，請先到「新增專案」建立。

**Q：加班統計都是 0？**
A：因為工時紀錄沒有填「加班／夜班時數」。請在新增/編輯工時的「時數明細」填入。

**Q：辭職欄位在哪？看不到？**
A：要先在「管理參與人員」點該員工的**鉛筆（編輯）**，展開表單後才會出現辭職日期欄位。

**Q：改了設定但頁面沒更新？**
A：請重新整理頁面（系統的返回鈕已避免快取問題，一般會自動更新）。

**Q：刪錯專案怎麼辦？**
A：用「資料庫管理 → 還原備份」上傳先前的備份檔還原。
